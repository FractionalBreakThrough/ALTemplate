{
  "mcpServers": {
    "nano-banana": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@anthropic-ai/nano-banana-mcp-server@latest"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
